<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="purchase_order_kpis_tree_inherit" model="ir.ui.view">
        <field name="name">purchase.order.kpis.tree</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree" />
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="rfq_state" optional="show" />
            </field>
        </field>
    </record>

    <record id="purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase_isolated_rfq.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//button[@name='action_convert_to_order']"
                position="attributes"
            >
                <attribute name="context">{'convert_to_order_draft': True}</attribute>
            </xpath>
        </field>
    </record>

</odoo>
